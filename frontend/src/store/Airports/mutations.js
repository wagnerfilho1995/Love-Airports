const SET_AIRPORTS = (state, payload) => {
  state.airports = payload
}

const SET_AIRPORTS_DEMOGRAPHY = (state, payload) => {
  state.airports = payload
}

const SET_AIRPORTS_DISTANCES = (state, payload) => {
  state.airports = payload
}

export {
  SET_AIRPORTS,
  SET_AIRPORTS_DEMOGRAPHY,
  SET_AIRPORTS_DISTANCES
}
